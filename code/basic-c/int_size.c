#include <limits.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
  printf("INT_SIZE: %d byte(s)\n", sizeof(int));
  printf("INT_MAX: %d\n", INT_MAX);
  printf("INT_MIN: %d\n", INT_MIN);
  return 0;
}
